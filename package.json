{
  "name": "mernapp",
  "version": "1.0.0",
  "description": "mern app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"  cd server && npm run dev  \"       \"   cd client && npm start   \"  ",
    "heroku-postbuild":"NPM_CONFIG_PRODCTION=false npm install --prefix client && npm run build --prefix client && --prefix server && npm install --prefix server"
  },
  "author": "Hcihem Hanafi",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.0.0",
    "prop-types": "^15.7.2"
  }
}
